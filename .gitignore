# Local flash state (per-machine, written by flash.sh)
.flash_state

# Editor/OS noise
.DS_Store
*.bak

# Arduino build output
build/

# Release artifacts (attached to GitHub releases, not committed)
release/

# Xcode noise (the .xcodeproj itself is committed)
xcuserdata/
*.xcuserstate
app/DerivedData/

*.xcarchive
*.ipa
*.dSYM
*.p8
*.p12
*.mobileprovision
*.cer
*.certSigningRequest
*.keychain
.env
*.mp4
*.MP4
*.mov
